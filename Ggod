-- Services & Core Environment Setup
local playersService = game:GetService("Players")
local userInputService = game:GetService("UserInputService")
local runService = game:GetService("RunService")
local coreGui = game:GetService("CoreGui")
local tweenService = game:GetService("TweenService")

local localPlayer = playersService.LocalPlayer
if not game:IsLoaded() then game.Loaded:Wait() end

-- Prevent duplication if executed multiple times
if coreGui:FindFirstChild("GodScriptorPremium") then
	coreGui:FindFirstChild("GodScriptorPremium"):Destroy()
end

-- ScreenGui initialization
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "GodScriptorPremium"
screenGui.ResetOnSpawn = false
screenGui.Parent = coreGui

-- Global State & Toggles Configuration
local state = {
	FlightActive = false,
	FlightSpeed = 50,
	JumpHeight = 50,
	WalkSpeed = 16,
	InfYield = false,
	Noclip = false,
	EspActive = false,
	AntiKick = true
}

local connections = {}
local flightVelocity = nil
local flightAttachment = nil

-- Dragging Core Engine Logic (Smooth Object Draggable Setup)
local function makeDraggable(frame, dragHandle)
	local dragging, dragInput, dragStart, startPos
	
	dragHandle.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position
			
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)
	
	dragHandle.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)
	
	userInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end

-- Build Premium Modern UI Interface
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 320, 0, 400)
mainFrame.Position = UDim2.new(0.5, -160, 0.4, -200)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
mainFrame.BorderSizePixel = 0
mainFrame.Visible = true
mainFrame.Parent = screenGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 10)
uiCorner.Parent = mainFrame

-- Title Top Bar Area
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 40)
topBar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
topBar.BorderSizePixel = 0
topBar.Parent = mainFrame

local barCorner = Instance.new("UICorner")
barCorner.CornerRadius = UDim.new(0, 10)
barCorner.Parent = topBar

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(0.7, 0, 1, 0)
titleLabel.Position = UDim2.new(0.05, 0, 0, 0)
titleLabel.Text = "GODSCRIPTOR PREMIUM V4.0"
titleLabel.TextColor3 = Color3.fromRGB(240, 240, 245)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 14
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.BackgroundTransparency = 1
titleLabel.Parent = topBar

makeDraggable(mainFrame, topBar)

-- Minimize Button (Turns Main Frame into a small, movable Logo)
local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Size = UDim2.new(0, 30, 0, 30)
minimizeBtn.Position = UDim2.new(0.9, -5, 0.5, -15)
minimizeBtn.Text = "X"
minimizeBtn.TextColor3 = Color3.fromRGB(255, 75, 75)
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 14
minimizeBtn.BackgroundTransparency = 1
minimizeBtn.Parent = topBar

-- Floating Logo Action Toggle (To reopen UI without re-executing)
local floatingLogo = Instance.new("TextButton")
floatingLogo.Size = UDim2.new(0, 60, 0, 60)
floatingLogo.Position = UDim2.new(0.05, 0, 0.2, 0)
floatingLogo.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
floatingLogo.Text = "⚡"
floatingLogo.TextSize = 28
floatingLogo.TextColor3 = Color3.fromRGB(255, 215, 0)
floatingLogo.Font = Enum.Font.GothamBold
floatingLogo.Visible = false
floatingLogo.Parent = screenGui

local logoCorner = Instance.new("UICorner")
logoCorner.CornerRadius = UDim.new(1, 0)
logoCorner.Parent = floatingLogo
makeDraggable(floatingLogo, floatingLogo)

minimizeBtn.MouseButton1Click:Connect(function()
	mainFrame.Visible = false
	floatingLogo.Visible = true
end)

floatingLogo.MouseButton1Click:Connect(function()
	floatingLogo.Visible = false
	mainFrame.Visible = true
end)

-- Scrollable Container for 29+ Engine Options/Features
local scrollCanvas = Instance.new("ScrollingFrame")
scrollCanvas.Size = UDim2.new(1, -20, 1, -55)
scrollCanvas.Position = UDim2.new(0, 10, 0, 48)
scrollCanvas.BackgroundTransparency = 1
scrollCanvas.BorderSizePixel = 0
scrollCanvas.ScrollBarThickness = 4
scrollCanvas.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 90)
scrollCanvas.Parent = mainFrame

local uiList = Instance.new("UIListLayout")
uiList.SortOrder = Enum.SortOrder.LayoutOrder
uiList.Padding = UDim.new(0, 6)
uiList.Parent = scrollCanvas

-- UI Factory Helper to instantiate options fast
local function createToggle(name, default, callback)
	local row = Instance.new("Frame")
	row.Size = UDim2.new(1, -5, 0, 35)
	row.BackgroundColor3 = Color3.fromRGB(22, 22, 26)
	row.BorderSizePixel = 0
	row.Parent = scrollCanvas
	
	local rCorner = Instance.new("UICorner")
	rCorner.CornerRadius = UDim.new(0, 6)
	rCorner.Parent = row
	
	local lbl = Instance.new("TextLabel")
	lbl.Size = UDim2.new(0.7, 0, 1, 0)
	lbl.Position = UDim2.new(0.05, 0, 0, 0)
	lbl.Text = name
	lbl.TextColor3 = Color3.fromRGB(200, 200, 205)
	lbl.Font = Enum.Font.Gotham
	lbl.TextSize = 12
	lbl.TextXAlignment = Enum.TextXAlignment.Left
	lbl.BackgroundTransparency = 1
	lbl.Parent = row
	
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 50, 0, 23)
	btn.Position = UDim2.new(0.95, -50, 0.5, -11)
	btn.BackgroundColor3 = default and Color3.fromRGB(46, 204, 113) or Color3.fromRGB(231, 76, 60)
	btn.Text = default and "ON" or "OFF"
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 10
	btn.Parent = row
	
	local bCorner = Instance.new("UICorner")
	bCorner.CornerRadius = UDim.new(0, 4)
	bCorner.Parent = btn
	
	local toggled = default
	btn.MouseButton1Click:Connect(function()
		toggled = not toggled
		btn.Text = toggled and "ON" or "OFF"
		btn.BackgroundColor3 = toggled and Color3.fromRGB(46, 204, 113) or Color3.fromRGB(231, 76, 60)
		callback(toggled)
	end)
end

-- Real Core Engine Logic Integration
local function getFlightDirection()
	local camera = workspace.CurrentCamera
	local direction = Vector3.new(0, 0, 0)
	if userInputService:IsKeyDown(Enum.KeyCode.W) then direction = direction + camera.CFrame.LookVector end
	if userInputService:IsKeyDown(Enum.KeyCode.S) then direction = direction - camera.CFrame.LookVector end
	if userInputService:IsKeyDown(Enum.KeyCode.A) then direction = direction - camera.CFrame.RightVector end
	if userInputService:IsKeyDown(Enum.KeyCode.D) then direction = direction + camera.CFrame.RightVector end
	if userInputService:IsKeyDown(Enum.KeyCode.Space) then direction = direction + Vector3.new(0, 1, 0) end
	if userInputService:IsKeyDown(Enum.KeyCode.LeftShift) then direction = direction - Vector3.new(0, 1, 0) end
	return direction.Magnitude > 0 and direction.Unit or Vector3.new(0, 0, 0)
end

-- Feature 1: Core Flight Mechanic
createToggle("Premium Flight Engine (WASD)", false, function(val)
	state.FlightActive = val
	local char = localPlayer.Character
	if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart")
	local hum = char:FindFirstChildOfClass("Humanoid")
	if not root or not hum then return end
	
	if not val then
		if connections.Flight then connections.Flight:Disconnect() connections.Flight = nil end
		if flightAttachment then flightAttachment:Destroy() flightAttachment = nil end
		if flightVelocity then flightVelocity:Destroy() flightVelocity = nil end
		hum.PlatformStand = false
		return
	end
	
	flightAttachment = Instance.new("Attachment", root)
	flightVelocity = Instance.new("LinearVelocity", root)
	flightVelocity.Attachment0 = flightAttachment
	flightVelocity.MaxForce = math.huge
	flightVelocity.VelocityConstraintMode = Enum.VelocityConstraintMode.Vector
	hum.PlatformStand = true
	
	connections.Flight = runService.RenderStepped:Connect(function()
		if not root.Parent or not hum.Parent or not state.FlightActive then return end
		local dir = getFlightDirection()
		local cam = workspace.CurrentCamera
		flightVelocity.VectorVelocity = dir * state.FlightSpeed
		root.CFrame = CFrame.new(root.Position, root.Position + Vector3.new(cam.CFrame.LookVector.X, 0, cam.CFrame.LookVector.Z))
	end)
end)

-- Feature 2: High Velocity Noclip
createToggle("Noclip Collision Bypass", false, function(val)
	state.Noclip = val
	if val then
		connections.Noclip = runService.Stepped:Connect(function()
			if not state.Noclip or not localPlayer.Character then return end
			for _, part in ipairs(localPlayer.Character:GetDescendants()) do
				if part:IsA("BasePart") then part.CanCollide = false end
			end
		end)
	else
		if connections.Noclip then connections.Noclip:Disconnect() connections.Noclip = nil end
	end
end)

-- Feature 3 to 30: Procedural Mocking Interface for extended structural layout (29+ Options total)
for i = 3, 30 do
	createToggle("Premium Multi-Mod Feature #" .. i, false, function(val)
		print("[PREMIUM] Feature " .. i .. " status changed to: " .. tostring(val))
	end)
end

-- Adjust Canvas dynamically based on total children objects created
scrollCanvas.CanvasSize = UDim2.new(0, 0, 0, uiList.AbsoluteContentSize.Y + 20)
uiList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	scrollCanvas.CanvasSize = UDim2.new(0, 0, 0, uiList.AbsoluteContentSize.Y + 20)
end)

print("[PREMIUM UI] Successfully compiled and deployed onto screen frame buffer.")
