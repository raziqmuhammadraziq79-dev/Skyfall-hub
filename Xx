-- ⚡ ROBLOX ESP AIMBOT ULTIMATE - PREMIUM EDITION
-- Compatibility: Delta Executor (Mobile)
-- Creator: wleo
-- Version: 2.0 ENHANCED

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local CONFIG = {
    ESP_ENABLED = true,
    AIMBOT_ENABLED = true,
    AIMBOT_ASSIST = true,
    PANEL_VISIBLE = true,
    ESP_DISTANCE = 500,
    AIM_SMOOTHNESS = 0.08,
    AIM_ASSIST_RANGE = 50,
    LOCK_NEAREST = true,
    TARGET_HEAD = true,
    SHOW_DISTANCE = true,
    SHOW_HEALTH = true,
    TRACERS_ENABLED = true,
    BOX_ESP = true,
    PREDICTION_ENABLED = true,
}

local COLORS = {
    ENEMY = Color3.fromRGB(255, 50, 50),
    DISTANCE_FAR = Color3.fromRGB(255, 100, 100),
    DISTANCE_NEAR = Color3.fromRGB(255, 0, 0),
    TEXT = Color3.fromRGB(255, 255, 255),
    PANEL_PRIMARY = Color3.fromRGB(15, 15, 25),
    PANEL_ACCENT = Color3.fromRGB(255, 50, 50),
    BUTTON_ACTIVE = Color3.fromRGB(50, 180, 100),
    BUTTON_INACTIVE = Color3.fromRGB(180, 50, 50),
}

local ESP_OBJECTS = {}
local CURRENT_TARGET = nil
local TEAM_CACHE = {}

-- ═══════════════════════════════════════════════
-- 🎯 TEAM DETECTION SYSTEM
-- ═══════════════════════════════════════════════

local function GetPlayerTeam(player)
    if player:FindFirstChild("Team") then
        return player.Team.Value
    elseif player:FindFirstChildOfClass("Team") then
        return player:FindFirstChildOfClass("Team")
    elseif player.Character and player.Character:FindFirstChild("Humanoid") then
        local humanoid = player.Character.Humanoid
        if humanoid:FindFirstChild("Team") then
            return humanoid.Team.Value
        end
    end
    return nil
end

local function IsEnemy(player)
    if player == LocalPlayer then return false end
    
    local localTeam = GetPlayerTeam(LocalPlayer)
    local playerTeam = GetPlayerTeam(player)
    
    if localTeam and playerTeam then
        return localTeam ~= playerTeam
    end
    
    return true
end

-- ═══════════════════════════════════════════════
-- 🔷 ADVANCED ESP RENDERER
-- ═══════════════════════════════════════════════

local function CreateAdvancedESP(player, color)
    local character = player.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then
        return nil
    end

    local espGroup = Instance.new("Model")
    espGroup.Name = "ESP_Group_" .. player.Name
    espGroup.Parent = workspace

    -- Diamond Kristal Utama
    local mainDiamond = Instance.new("Part")
    mainDiamond.Name = "MainDiamond"
    mainDiamond.Shape = Enum.PartType.Ball
    mainDiamond.CanCollide = false
    mainDiamond.CFrame = character.HumanoidRootPart.CFrame
    mainDiamond.Size = Vector3.new(0.5, 0.5, 0.5)
    mainDiamond.Color = color
    mainDiamond.Material = Enum.Material.Neon
    mainDiamond.TopSurface = Enum.SurfaceType.Smooth
    mainDiamond.BottomSurface = Enum.SurfaceType.Smooth
    mainDiamond.Parent = espGroup

    local weld1 = Instance.new("WeldConstraint")
    weld1.Part0 = character.HumanoidRootPart
    weld1.Part1 = mainDiamond
    weld1.Parent = mainDiamond

    -- Box Outline
    local boxFrame = Instance.new("Part")
    boxFrame.Name = "BoxFrame"
    boxFrame.Shape = Enum.PartType.Block
    boxFrame.CanCollide = false
    boxFrame.Size = Vector3.new(2, 3, 2)
    boxFrame.Color = color
    boxFrame.Transparency = 0.7
    boxFrame.Material = Enum.Material.ForceField
    boxFrame.TopSurface = Enum.SurfaceType.Smooth
    boxFrame.BottomSurface = Enum.SurfaceType.Smooth
    boxFrame.Parent = espGroup

    local weld2 = Instance.new("WeldConstraint")
    weld2.Part0 = character.HumanoidRootPart
    weld2.Part1 = boxFrame
    weld2.Parent = boxFrame

    -- Head Indicator
    if character:FindFirstChild("Head") then
        local headMarker = Instance.new("Part")
        headMarker.Name = "HeadMarker"
        headMarker.Shape = Enum.PartType.Ball
        headMarker.CanCollide = false
        headMarker.Size = Vector3.new(0.8, 0.8, 0.8)
        headMarker.Color = Color3.fromRGB(255, 200, 0)
        headMarker.Material = Enum.Material.Neon
        headMarker.Transparency = 0.3
        headMarker.Parent = espGroup

        local weld3 = Instance.new("WeldConstraint")
        weld3.Part0 = character.Head
        weld3.Part1 = headMarker
        weld3.Parent = headMarker
    end

    -- Status Label
    local statusLabel = Instance.new("Part")
    statusLabel.Name = "StatusLabel"
    statusLabel.CanCollide = false
    statusLabel.Size = Vector3.new(1, 1, 0.1)
    statusLabel.Color = Color3.fromRGB(0, 0, 0)
    statusLabel.Transparency = 0.5
    statusLabel.Parent = espGroup

    local weld4 = Instance.new("WeldConstraint")
    weld4.Part0 = character.HumanoidRootPart
    weld4.Part1 = statusLabel
    weld4.Parent = statusLabel
    statusLabel.CFrame = character.HumanoidRootPart.CFrame + Vector3.new(0, 4, 0)

    local textGui = Instance.new("SurfaceGui")
    textGui.Face = Enum.NormalId.Front
    textGui.Parent = statusLabel

    local playerNameLabel = Instance.new("TextLabel")
    playerNameLabel.Name = "PlayerName"
    playerNameLabel.Size = UDim2.new(1, 0, 0.5, 0)
    playerNameLabel.BackgroundTransparency = 1
    playerNameLabel.TextColor3 = COLORS.TEXT
    playerNameLabel.TextSize = 14
    playerNameLabel.Font = Enum.Font.GothamBold
    playerNameLabel.Text = player.Name
    playerNameLabel.Parent = textGui

    local healthLabel = Instance.new("TextLabel")
    healthLabel.Name = "Health"
    healthLabel.Size = UDim2.new(1, 0, 0.5, 0)
    healthLabel.Position = UDim2.new(0, 0, 0.5, 0)
    healthLabel.BackgroundTransparency = 1
    healthLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
    healthLabel.TextSize = 12
    healthLabel.Font = Enum.Font.Gotham
    healthLabel.Text = "HP: 100"
    healthLabel.Parent = textGui

    local distanceLabel = Instance.new("TextLabel")
    distanceLabel.Name = "Distance"
    distanceLabel.Size = UDim2.new(1, 0, 0.5, 0)
    distanceLabel.Position = UDim2.new(0, 0, 1, 0)
    distanceLabel.BackgroundTransparency = 1
    distanceLabel.TextColor3 = Color3.fromRGB(150, 150, 255)
    distanceLabel.TextSize = 12
    distanceLabel.Font = Enum.Font.Gotham
    distanceLabel.Text = "0m"
    distanceLabel.Parent = textGui

    return {
        Group = espGroup,
        MainDiamond = mainDiamond,
        BoxFrame = boxFrame,
        StatusLabel = statusLabel,
        PlayerNameLabel = playerNameLabel,
        HealthLabel = healthLabel,
        DistanceLabel = distanceLabel,
    }
end

-- ═══════════════════════════════════════════════
-- 👁️ UPDATE ESP VISUAL
-- ═══════════════════════════════════════════════

local function UpdateESP()
    if not CONFIG.ESP_ENABLED then return end

    local localHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not localHRP then return end

    for player, espData in pairs(ESP_OBJECTS) do
        if not player.Parent or not player.Character then
            if espData.Group then espData.Group:Destroy() end
            ESP_OBJECTS[player] = nil
        else
            local character = player.Character
            if character:FindFirstChild("HumanoidRootPart") and character:FindFirstChild("Humanoid") then
                local hrp = character.HumanoidRootPart
                local humanoid = character.Humanoid
                local distance = (hrp.Position - localHRP.Position).Magnitude

                if distance > CONFIG.ESP_DISTANCE then
                    espData.Group.Parent = nil
                else
                    espData.Group.Parent = workspace

                    -- Update Health Label
                    if CONFIG.SHOW_HEALTH then
                        local health = math.ceil(humanoid.Health)
                        espData.HealthLabel.Text = "HP: " .. health
                        if health > 50 then
                            espData.HealthLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
                        elseif health > 25 then
                            espData.HealthLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
                        else
                            espData.HealthLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
                        end
                    end

                    -- Update Distance Label
                    if CONFIG.SHOW_DISTANCE then
                        espData.DistanceLabel.Text = string.format("%.1fm", distance)
                    end

                    -- Color change based on distance
                    local distColor = distance < 100 and COLORS.DISTANCE_NEAR or COLORS.DISTANCE_FAR
                    espData.MainDiamond.Color = distColor
                    espData.BoxFrame.Color = distColor
                end
            end
        end
    end

    for _, player in pairs(Players:GetPlayers()) do
        if not IsEnemy(player) then continue end
        if player == LocalPlayer then continue end

        local character = player.Character
        if not character or not character:FindFirstChild("HumanoidRootPart") then continue end

        if not ESP_OBJECTS[player] then
            local espData = CreateAdvancedESP(player, COLORS.ENEMY)
            ESP_OBJECTS[player] = espData
        end
    end
end

-- ═══════════════════════════════════════════════
-- 🎯 TRACER LINES
-- ═══════════════════════════════════════════════

local function DrawTracers()
    if not CONFIG.TRACERS_ENABLED then return end

    local localHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not localHRP then return end

    for player, espData in pairs(ESP_OBJECTS) do
        if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then continue end

        local targetHRP = player.Character.HumanoidRootPart
        local screenPos, onScreen = Camera:WorldToScreenPoint(targetHRP.Position)

        if onScreen then
            local line = Drawing.new("Line")
            line.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
            line.To = Vector2.new(screenPos.X, screenPos.Y)
            line.Color = COLORS.ENEMY
            line.Thickness = 1
            line.Transparency = 0.7
            task.wait(0.01)
            line:Remove()
        end
    end
end

-- ═══════════════════════════════════════════════
-- 🔍 FIND NEAREST ENEMY WITH PREDICTION
-- ═══════════════════════════════════════════════

local function FindNearestTarget()
    local nearestPlayer = nil
    local shortestDistance = math.huge
    local localHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

    if not localHRP then return nil end

    for _, player in pairs(Players:GetPlayers()) do
        if not IsEnemy(player) then continue end
        if player == LocalPlayer then continue end

        local character = player.Character
        if not character or not character:FindFirstChild("Humanoid") then continue end
        if character.Humanoid.Health <= 0 then continue end

        local targetHRP = character:FindFirstChild("HumanoidRootPart")
        if not targetHRP then continue end

        local distance = (targetHRP.Position - localHRP.Position).Magnitude

        if distance < shortestDistance and distance <= CONFIG.ESP_DISTANCE then
            shortestDistance = distance
            nearestPlayer = player
        end
    end

    return nearestPlayer
end

-- ═══════════════════════════════════════════════
-- 🔒 AIMBOT ASSIST (SOFT LOCK)
-- ═══════════════════════════════════════════════

local function AimbotAssist()
    if not CONFIG.AIMBOT_ENABLED then return end

    CURRENT_TARGET = FindNearestTarget()

    if not CURRENT_TARGET or not CURRENT_TARGET.Character then
        return
    end

    local targetCharacter = CURRENT_TARGET.Character
    local targetPart = CONFIG.TARGET_HEAD and targetCharacter:FindFirstChild("Head") or targetCharacter:FindFirstChild("HumanoidRootPart")

    if not targetPart then return end

    local targetPosition = targetPart.Position
    local cameraPosition = Camera.CFrame.Position
    
    -- Prediction untuk target yang bergerak
    if CONFIG.PREDICTION_ENABLED and targetCharacter:FindFirstChild("HumanoidRootPart") then
        local humanoidRootPart = targetCharacter.HumanoidRootPart
        local velocity = humanoidRootPart.AssemblyLinearVelocity
        local distance = (targetPosition - cameraPosition).Magnitude
        local travelTime = distance / 100
        targetPosition = targetPosition + (velocity * travelTime)
    end

    local direction = (targetPosition - cameraPosition).Unit
    local newCFrame = CFrame.new(cameraPosition, cameraPosition + direction)
    
    -- Soft lock - tidak sepenuhnya mengunci
    Camera.CFrame = Camera.CFrame:Lerp(newCFrame, CONFIG.AIM_SMOOTHNESS)
end

-- ═══════════════════════════════════════════════
-- 🎛️ PREMIUM UI PANEL
-- ═══════════════════════════════════════════════

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PremiumPanel"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Main Panel
local MainPanel = Instance.new("Frame")
MainPanel.Name = "MainPanel"
MainPanel.Size = UDim2.new(0, 320, 0, 500)
MainPanel.Position = UDim2.new(0, 20, 0, 20)
MainPanel.BackgroundColor3 = COLORS.PANEL_PRIMARY
MainPanel.BorderSizePixel = 2
MainPanel.BorderColor3 = COLORS.PANEL_ACCENT
MainPanel.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainPanel

-- Header
local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Size = UDim2.new(1, 0, 0, 45)
Header.BackgroundColor3 = COLORS.PANEL_ACCENT
Header.BorderSizePixel = 0
Header.Parent = MainPanel

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 8)
HeaderCorner.Parent = Header

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "Title"
TitleLabel.Size = UDim2.new(0.7, 0, 1, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 18
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "🔷 ESP AIMBOT PRO"
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = Header

-- Close Button Header
local CloseHeaderBtn = Instance.new("TextButton")
CloseHeaderBtn.Name = "CloseHeader"
CloseHeaderBtn.Size = UDim2.new(0, 35, 1, 0)
CloseHeaderBtn.Position = UDim2.new(0.85, 0, 0, 0)
CloseHeaderBtn.BackgroundTransparency = 0.5
CloseHeaderBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseHeaderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseHeaderBtn.TextSize = 16
CloseHeaderBtn.Font = Enum.Font.GothamBold
CloseHeaderBtn.Text = "×"
CloseHeaderBtn.Parent = Header

CloseHeaderBtn.MouseButton1Click:Connect(function()
    MainPanel.Visible = not MainPanel.Visible
end)

-- Content Area
local ContentFrame = Instance.new("ScrollingFrame")
ContentFrame.Name = "Content"
ContentFrame.Size = UDim2.new(1, -10, 1, -60)
ContentFrame.Position = UDim2.new(0, 5, 0, 50)
ContentFrame.BackgroundTransparency = 1
ContentFrame.BorderSizePixel = 0
ContentFrame.ScrollBarThickness = 6
ContentFrame.Parent = MainPanel

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 8)
UIListLayout.Parent = ContentFrame

-- Function to create toggle button
local function CreateToggle(text, initialState, callback)
    local toggleFrame = Instance.new("Frame")
    toggleFrame.Name = text .. "Toggle"
    toggleFrame.Size = UDim2.new(1, 0, 0, 40)
    toggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    toggleFrame.BorderSizePixel = 1
    toggleFrame.BorderColor3 = Color3.fromRGB(80, 80, 100)
    toggleFrame.Parent = ContentFrame

    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(0, 6)
    toggleCorner.Parent = toggleFrame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = COLORS.TEXT
    label.TextSize = 14
    label.Font = Enum.Font.Gotham
    label.Text = text
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = toggleFrame

    local toggleButton = Instance.new("TextButton")
    toggleButton.Name = "Toggle"
    toggleButton.Size = UDim2.new(0.35, -8, 0.8, 0)
    toggleButton.Position = UDim2.new(0.62, 0, 0.1, 0)
    toggleButton.BackgroundColor3 = initialState and COLORS.BUTTON_ACTIVE or COLORS.BUTTON_INACTIVE
    toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleButton.TextSize = 12
    toggleButton.Font = Enum.Font.GothamBold
    toggleButton.Text = initialState and "ON" or "OFF"
    toggleButton.Parent = toggleFrame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 4)
    btnCorner.Parent = toggleButton

    local state = initialState
    toggleButton.MouseButton1Click:Connect(function()
        state = not state
        toggleButton.Text = state and "ON" or "OFF"
        toggleButton.BackgroundColor3 = state and COLORS.BUTTON_ACTIVE or COLORS.BUTTON_INACTIVE
        callback(state)
    end)

    return toggleFrame
end

-- Function to create slider
local function CreateSlider(text, minVal, maxVal, currentVal, callback)
    local sliderFrame = Instance.new("Frame")
    sliderFrame.Name = text .. "Slider"
    sliderFrame.Size = UDim2.new(1, 0, 0, 50)
    sliderFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    sliderFrame.BorderSizePixel = 1
    sliderFrame.BorderColor3 = Color3.fromRGB(80, 80, 100)
    sliderFrame.Parent = ContentFrame

    local sliderCorner = Instance.new("UICorner")
    sliderCorner.CornerRadius = UDim.new(0, 6)
    sliderCorner.Parent = sliderFrame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0.4, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = COLORS.TEXT
    label.TextSize = 13
    label.Font = Enum.Font.GothamBold
    label.Text = text .. ": " .. currentVal
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = sliderFrame

    local sliderBar = Instance.new("Frame")
    sliderBar.Size = UDim2.new(1, -10, 0, 6)
    sliderBar.Position = UDim2.new(0, 5, 0.5, 0)
    sliderBar.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    sliderBar.BorderSizePixel = 0
    sliderBar.Parent = sliderFrame

    local sliderBarCorner = Instance.new("UICorner")
    sliderBarCorner.CornerRadius = UDim.new(0, 3)
    sliderBarCorner.Parent = sliderBar

    local sliderButton = Instance.new("TextButton")
    sliderButton.Size = UDim2.new(0, 14, 0, 14)
    sliderButton.Position = UDim2.new((currentVal - minVal) / (maxVal - minVal), -7, 0.5, -7)
    sliderButton.BackgroundColor3 = COLORS.PANEL_ACCENT
    sliderButton.BorderSizePixel = 0
    sliderButton.Text = ""
    sliderButton.Parent = sliderBar

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 7)
    btnCorner.Parent = sliderButton

    local dragging = false

    sliderButton.MouseButton1Down:Connect(function()
        dragging = true
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local mousePos = UserInputService:GetMouseLocation()
            local sliderPos = sliderBar.AbsolutePosition.X
            local sliderSize = sliderBar.AbsoluteSize.X
            local relativePos = math.clamp(mousePos.X - sliderPos, 0, sliderSize)
            local percentage = relativePos / sliderSize
            local newVal = math.floor(minVal + (maxVal - minVal) * percentage)

            sliderButton.Position = UDim2.new(percentage, -7, 0.5, -7)
            label.Text = text .. ": " .. newVal
            callback(newVal)
        end
    end)

    return sliderFrame
end

-- Add Toggle Controls
CreateToggle("ESP", CONFIG.ESP_ENABLED, function(state)
    CONFIG.ESP_ENABLED = state
end)

CreateToggle("Aimbot", CONFIG.AIMBOT_ENABLED, function(state)
    CONFIG.AIMBOT_ENABLED = state
end)

CreateToggle("Aimbot Assist", CONFIG.AIMBOT_ASSIST, function(state)
    CONFIG.AIMBOT_ASSIST = state
end)

CreateToggle("Target Head", CONFIG.TARGET_HEAD, function(state)
    CONFIG.TARGET_HEAD = state
end)

CreateToggle("Show Distance", CONFIG.SHOW_DISTANCE, function(state)
    CONFIG.SHOW_DISTANCE = state
end)

CreateToggle("Show Health", CONFIG.SHOW_HEALTH, function(state)
    CONFIG.SHOW_HEALTH = state
end)

CreateToggle("Tracers", CONFIG.TRACERS_ENABLED, function(state)
    CONFIG.TRACERS_ENABLED = state
end)

CreateToggle("Prediction", CONFIG.PREDICTION_ENABLED, function(state)
    CONFIG.PREDICTION_ENABLED = state
end)

-- Add Slider Controls
CreateSlider("ESP Distance", 100, 1000, CONFIG.ESP_DISTANCE, function(val)
    CONFIG.ESP_DISTANCE = val
end)

CreateSlider("Aim Smoothness", 0.01, 0.5, CONFIG.AIM_SMOOTHNESS, function(val)
    CONFIG.AIM_SMOOTHNESS = val / 100
end)

CreateSlider("Aim Assist Range", 10, 100, CONFIG.AIM_ASSIST_RANGE, function(val)
    CONFIG.AIM_ASSIST_RANGE = val
end)

-- Footer
local FooterFrame = Instance.new("Frame")
FooterFrame.Name = "Footer"
FooterFrame.Size = UDim2.new(1, 0, 0, 45)
FooterFrame.Position = UDim2.new(0, 0, 1, -45)
FooterFrame.BackgroundColor3 = COLORS.PANEL_ACCENT
FooterFrame.BorderSizePixel = 0
FooterFrame.Parent = MainPanel

local FooterCorner = Instance.new("UICorner")
FooterCorner.CornerRadius = UDim.new(0, 8)
FooterCorner.Parent = FooterFrame

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Name = "Status"
StatusLabel.Size = UDim2.new(1, 0, 1, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
StatusLabel.TextSize = 12
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.Text = "Status: Ready | Target: None"
StatusLabel.Parent = FooterFrame

-- ═══════════════════════════════════════════════
-- ⌨️ KEYBOARD SHORTCUTS
-- ═══════════════════════════════════════════════

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end

    if input.KeyCode == Enum.KeyCode.E then
        CONFIG.ESP_ENABLED = not CONFIG.ESP_ENABLED
    elseif input.KeyCode == Enum.KeyCode.R then
        CONFIG.AIMBOT_ENABLED = not CONFIG.AIMBOT_ENABLED
    elseif input.KeyCode == Enum.KeyCode.T then
        CONFIG.TRACERS_ENABLED = not CONFIG.TRACERS_ENABLED
    elseif input.KeyCode == Enum.KeyCode.P then
        MainPanel.Visible = not MainPanel.Visible
    end
end)

-- ═══════════════════════════════════════════════
-- 🔄 MAIN LOOP
-- ═══════════════════════════════════════════════

RunService.RenderStepped:Connect(function()
    if CONFIG.ESP_ENABLED then
        UpdateESP()
    end
    
    if CONFIG.AIMBOT_ENABLED and CONFIG.AIMBOT_ASSIST then
        AimbotAssist()
    end

    if CONFIG.TRACERS_ENABLED then
        DrawTracers()
    end

    -- Update Status
    local targetName = CURRENT_TARGET and CURRENT_TARGET.Name or "None"
    StatusLabel.Text = "Status: Active | Target: " .. targetName
end)

-- ═══════════════════════════════════════════════
-- 🗑️ CLEANUP & SAFETY
-- ═══════════════════════════════════════════════

Players.PlayerRemoving:Connect(function(player)
    if ESP_OBJECTS[player] then
        if ESP_OBJECTS[player].Group then
            ESP_OBJECTS[player].Group:Destroy()
        end
        ESP_OBJECTS[player] = nil
    end
end)

game:GetService("RunService").Heartbeat:Connect(function()
    if not LocalPlayer.Parent then
        for player, espData in pairs(ESP_OBJECTS) do
            if espData.Group then espData.Group:Destroy() end
        end
        ESP_OBJECTS = {}
    end
end)

print("✅ Premium ESP Aimbot Script Loaded Successfully")
print("📋 Shortcuts: E=ESP | R=Aimbot | T=Tracers | P=Panel")
