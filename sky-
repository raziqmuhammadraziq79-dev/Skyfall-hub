--[[
	---@module GodScriptorPremiumPanel
	---@author GODSCRIPTOR
	---@description Premium Multi-Feature Exploit Interface optimized for mobile/PC injection.
]]

-- Services
local playersService = game:GetService("Players")
local runService = game:GetService("RunService")
local workspace = game:GetService("Workspace")
local coreGui = game:GetService("CoreGui")
local userInputService = game:GetService("UserInputService")

-- State Management
local localPlayer = playersService.LocalPlayer
local camera = workspace.CurrentCamera
local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()

local States = {
	Fly = false,
	Noclip = false,
	InfJump = false,
	FlySpeed = 50
}

-- Physics References
local FlyBodyVelocity = nil
local FlyBodyGyro = nil
local Connections = {}

--------------------------------------------------------------------------------
-- CORE FEATURES CORE LOGIC
--------------------------------------------------------------------------------

-- Infinite Jump Hook
Connections.InfJump = userInputService.JumpRequest:Connect(function()
	if States.InfJump and localPlayer.Character then
		local humanoid = localPlayer.Character:FindFirstChildOfClass("Humanoid")
		if humanoid then
			humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
		end
	end
end)

-- Noclip Loop via Stepped (Fires before physics processing)
Connections.Noclip = runService.Stepped:Connect(function()
	if States.Noclip and localPlayer.Character then
		for _, part in ipairs(localPlayer.Character:GetDescendants()) do
			if part:IsA("BasePart") and part.CanCollide then
				part.CanCollide = false
			end
		end
	end
end)

-- Flight Execution System
local function toggleFlightEngine(enable)
	local char = localPlayer.Character
	if not char or not char:FindFirstChild("HumanoidRootPart") then return end
	local root = char.HumanoidRootPart
	local hum = char:FindFirstChildOfClass("Humanoid")
	
	if enable then
		FlyBodyVelocity = Instance.new("BodyVelocity")
		FlyBodyVelocity.MaxForce = Vector3.new(1e5, 1e5, 1e5)
		FlyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
		FlyBodyVelocity.Parent = root
		
		FlyBodyGyro = Instance.new("BodyGyro")
		FlyBodyGyro.MaxTorque = Vector3.new(1e5, 1e5, 1e5)
		FlyBodyGyro.CFrame = root.CFrame
		FlyBodyGyro.Parent = root
		
		if hum then hum.PlatformStand = true end
		
		Connections.FlightLoop = runService.RenderStepped:Connect(function()
			if not States.Fly or not localPlayer.Character then return end
			local moveDirection = hum and hum.MoveDirection or Vector3.new(0,0,0)
			local camCFrame = camera.CFrame
			
			if moveDirection.Magnitude > 0 then
				local dir = (camCFrame.LookVector * moveDirection.Z) + (camCFrame.RightVector * moveDirection.X)
				FlyBodyVelocity.Velocity = dir.Unit * States.FlySpeed
			else
				FlyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
			end
			FlyBodyGyro.CFrame = camCFrame
		end)
	else
		if Connections.FlightLoop then Connections.FlightLoop:Disconnect() end
		if FlyBodyVelocity then FlyBodyVelocity:Destroy() end
		if FlyBodyGyro then FlyBodyGyro:Destroy() end
		if hum then hum.PlatformStand = false end
	end
end

--------------------------------------------------------------------------------
-- PREMIUM UI FRAMEWORK ARCHITECTURE
--------------------------------------------------------------------------------

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "GodScriptor_Premium"
screenGui.ResetOnSpawn = false

-- Safe Injection Layer
if syn and syn.protect_gui then syn.protect_gui(screenGui) end
screenGui.Parent = getgui and getgui() or coreGui

-- Frame Utama (Sleek Cyberpunk Grid Dark)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 280, 0, 270)
mainFrame.Position = UDim2.new(0.5, -140, 0.3, -135)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 10)
corner.Parent = mainFrame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(0, 180, 216) -- Premium Neon Cyan Accent
stroke.Thickness = 2
stroke.Parent = mainFrame

-- Title Header
local header = Instance.new("TextLabel")
header.Size = UDim2.new(1, 0, 0, 40)
header.BackgroundTransparency = 1
header.Text = "⚡ GODSCRIPTOR PREMIUM ⚡"
header.TextColor3 = Color3.fromRGB(255, 255, 255)
header.Font = Enum.Font.Code
header.TextSize = 14
header.Parent = mainFrame

-- UI Content Layout List
local listLayout = Instance.new("UIListLayout")
listLayout.Padding = UDim.new(0, 8)
listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
listLayout.SortOrder = Enum.SortOrder.LayoutOrder

local container = Instance.new("Frame")
container.Size = UDim2.new(1, -24, 1, -50)
container.Position = UDim2.new(0, 12, 0, 40)
container.BackgroundTransparency = 1
container.Parent = mainFrame
listLayout.Parent = container

-- Helper Function to Create Premium Toggles
local function createToggle(name, layoutOrder, callback)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, 0, 0, 38)
	btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
	btn.Text = name .. " : OFF"
	btn.TextColor3 = Color3.fromRGB(242, 100, 100)
	btn.Font = Enum.Font.SourceSansBold
	btn.TextSize = 15
	btn.LayoutOrder = layoutOrder
	btn.Parent = container
	
	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 6)
	btnCorner.Parent = btn
	
	local active = false
	btn.MouseButton1Click:Connect(function()
		active = not active
		if active then
			btn.Text = name .. " : ACTIVE"
			btn.TextColor3 = Color3.fromRGB(100, 242, 100)
			btn.BackgroundColor3 = Color3.fromRGB(25, 35, 25)
		else
			btn.Text = name .. " : OFF"
			btn.TextColor3 = Color3.fromRGB(242, 100, 100)
			btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
		end
		callback(active)
	end)
end

-- Render Toggles
createToggle("FLIGHT ENGINE", 1, function(bool)
	States.Fly = bool
	toggleFlightEngine(bool)
end)

createToggle("NOCLIP (PHASE)", 2, function(bool)
	States.Noclip = bool
end)

createToggle("INFINITE JUMP", 3, function(bool)
	States.InfJump = bool
end)

-- Speed Slider Elements inside Container
local sliderLabel = Instance.new("TextLabel")
sliderLabel.Size = UDim2.new(1, 0, 0, 20)
sliderLabel.BackgroundTransparency = 1
sliderLabel.Text = "FLY SPEED: 50"
sliderLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
sliderLabel.Font = Enum.Font.SourceSans
sliderLabel.TextSize = 14
sliderLabel.LayoutOrder = 4
sliderLabel.Parent = container

local sliderTrack = Instance.new("TextButton")
sliderTrack.Size = UDim2.new(1, 0, 0, 8)
sliderTrack.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
sliderTrack.Text = ""
sliderTrack.LayoutOrder = 5
sliderTrack.Parent = container

local trackCorner = Instance.new("UICorner")
trackCorner.CornerRadius = UDim.new(0, 4)
trackCorner.Parent = sliderTrack

local sliderFill = Instance.new("Frame")
sliderFill.Size = UDim2.new(0.2, 0, 1, 0)
sliderFill.BackgroundColor3 = Color3.fromRGB(0, 180, 216)
sliderFill.BorderSizePixel = 0
sliderFill.Parent = sliderTrack

-- Slider Logic
local isDragging = false
local function updateSlider(input)
	local percentage = math.clamp((input.Position.X - sliderTrack.AbsolutePosition.X) / sliderTrack.AbsoluteSize.X, 0, 1)
	sliderFill.Size = UDim2.new(percentage, 0, 1, 0)
	States.FlySpeed = math.round(percentage * 300)
	sliderLabel.Text = "FLY SPEED: " .. tostring(States.FlySpeed)
end

sliderTrack.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		isDragging = true
		updateSlider(input)
	end
end)

userInputService.InputChanged:Connect(function(input)
	if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		updateSlider(input)
	end
end)

userInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		isDragging = false
	end
end)

-- Character Auto-Refresh Hook
localPlayer.CharacterAdded:Connect(function(newChar)
	character = newChar
	if States.Fly then
		toggleFlightEngine(false)
		task.wait(0.1)
		toggleFlightEngine(true)
	end
end)

print("[OUTPUT] Premium Multi-Hack Loaded Interface Engine.")
