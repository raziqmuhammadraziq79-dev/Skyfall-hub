-- Hanami Eclipse | Zero Clutter & Smart Lock Fix
-- Engine: Strict Player Validation & Collapsible UI
-- Status: Anti-False Positive & Sniper Compatible

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- ================= CONFIGURATION =================
local Config = {
    Enabled = true,
    MaxRenderDist = 300, -- ESP hilang jika > 300 stud (Fix Clutter)
    FOV = 250,
    ColorEnemy = Color3.fromRGB(255, 50, 50),
    ColorTeam = Color3.fromRGB(50, 200, 255),
    Thickness = 2
}

-- ================= STORAGE =================
local ESP_Cache = {}
local CurrentTarget = nil

-- ================= COLLAPSIBLE UI HUB =================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = game.CoreGui

-- Main Panel
local Panel = Instance.new("Frame")
Panel.Size = UDim2.new(0, 200, 0, 220)
Panel.Position = UDim2.new(0, 20, 0, 20)
Panel.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Panel.BackgroundTransparency = 0.1
Panel.BorderSizePixel = 0
Panel.Visible = true
Panel.Parent = ScreenGui
Instance.new("UICorner", Panel).CornerRadius = UDim.new(0, 10)

-- Header & Toggle
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 40)
Header.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Header.BorderSizePixel = 0
Header.Parent = Panel
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 10)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0.7, 0, 1, 0)
Title.Text = "ECLIPSE HUB"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 16
Title.BackgroundTransparency = 1
Title.Parent = Header

local CollapseBtn = Instance.new("TextButton")
CollapseBtn.Size = UDim2.new(0.3, 0, 1, 0)
CollapseBtn.Position = UDim2.new(0.7, 0, 0, 0)
CollapseBtn.BackgroundTransparency = 1
CollapseBtn.Text = "[-]"
CollapseBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
CollapseBtn.Font = Enum.Font.GothamBold
CollapseBtn.TextSize = 18
CollapseBtn.Parent = Header

local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, 0, 1, -40)
ContentFrame.Position = UDim2.new(0, 0, 0, 40)
ContentFrame.BackgroundTransparency = 1
ContentFrame.ClipsDescendants = true
ContentFrame.Parent = Panel

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(1, -20, 0, 45)
ToggleBtn.Position = UDim2.new(0, 10, 0, 10)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
ToggleBtn.Text = "SYSTEM ACTIVE"
ToggleBtn.TextColor3 = Color3.new(1,1,1)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.Parent = ContentFrame
Instance.new("UICorner", ToggleBtn)

local StatusTxt = Instance.new("TextLabel")
StatusTxt.Size = UDim2.new(1, 0, 0, 20)
StatusTxt.Position = UDim2.new(0, 0, 1, -20)
StatusTxt.BackgroundTransparency = 1
StatusTxt.Text = "Clean Mode On"
StatusTxt.TextColor3 = Color3.fromRGB(150, 150, 150)
StatusTxt.TextSize = 11
StatusTxt.Parent = ContentFrame

-- Logic Collapse
local IsCollapsed = false
CollapseBtn.MouseButton1Click:Connect(function()
    IsCollapsed = not IsCollapsed
    if IsCollapsed then
        Panel.Size = UDim2.new(0, 200, 0, 40)
        CollapseBtn.Text = "[+]"
        ContentFrame.Visible = false
    else
        Panel.Size = UDim2.new(0, 200, 0, 220)
        CollapseBtn.Text = "[-]"
        ContentFrame.Visible = true
    end
end)

local IsActive = true
ToggleBtn.MouseButton1Click:Connect(function()
    IsActive = not IsActive
    Config.Enabled = IsActive
    ToggleBtn.Text = IsActive and "SYSTEM ACTIVE" or "SYSTEM PAUSED"
    ToggleBtn.BackgroundColor3 = IsActive and Color3.fromRGB(0, 120, 255) or Color3.fromRGB(150, 0, 0)
end)

-- ================= STRICT ESP ENGINE (FIX BUG) =================
function CreateESP(Player)
    if Player == LocalPlayer then return end
    
    local Box = Drawing.new("Square")
    Box.Thickness = Config.Thickness
    Box.Filled = false
    
    local Tracer = Drawing.new("Line")
    Tracer.Thickness = 1.5
    
    ESP_Cache[Player] = { Box = Box, Tracer = Tracer }
end

function UpdateESP()
    if not Config.Enabled then
        for _, Data in pairs(ESP_Cache) do
            Data.Box.Visible = false
            Data.Tracer.Visible = false
        end
        return
    end

    for Player, Data in pairs(ESP_Cache) do
        -- VALIDASI KRITIS: Pastikan ini adalah Player Character asli
        if Player.Character and Player.Character:FindFirstChild("Humanoid") then
            local Root = Player.Character:FindFirstChild("HumanoidRootPart")
            local Head = Player.Character:FindFirstChild("Head")
            
            if Root and Head then
                local Pos, OnScreen = Camera:WorldToViewportPoint(Root.Position)
                local Dist = (Camera.CFrame.Position - Root.Position).Magnitude
                
                -- FILTER JARAK: Jangan render jika terlalu jauh (Fix Clutter)
                if OnScreen and Dist < Config.MaxRenderDist then
                    local HeadPos, _ = Camera:WorldToViewportPoint(Head.Position)
                    local Height = math.abs(HeadPos.Y - Pos.Y) * 2.2
                    local Width = Height * 0.6
                    
                    Data.Box.Size = Vector2.new(Width, Height)
                    Data.Box.Position = Vector2.new(Pos.X - Width/2, HeadPos.Y - Height/2 + 10)
                    
                    local IsEnemy = not (LocalPlayer.Team and Player.Team and LocalPlayer.Team == Player.Team)
                    local Color = IsEnemy and Config.ColorEnemy or Config.ColorTeam
                    
                    Data.Box.Color = Color
                    Data.Tracer.Color = Color
                    
                    -- TRACER FILTER: Hanya tampilkan tracer musuh dekat
                    if IsEnemy and Dist < 150 then
                        Data.Tracer.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
                        Data.Tracer.To = Vector2.new(Pos.X, Pos.Y)
                        Data.Tracer.Visible = true
                    else
                        Data.Tracer.Visible = false
                    end
                    
                    Data.Box.Visible = true
                else
                    Data.Box.Visible = false
                    Data.Tracer.Visible = false
                end
            end
        else
            -- Bersihkan ESP jika karakter hilang/respawn
            Data.Box.Visible = false
            Data.Tracer.Visible = false
        end
    end
end

-- ================= SMART NEAREST LOCK =================
function GetNearestTarget()
    local Closest = nil
    local MinDist = math.huge
    
    for _, Player in pairs(Players:GetPlayers()) do
        if Player ~= LocalPlayer and Player.Character and Player.Character:FindFirstChild("Humanoid") then
            local Head = Player.Character:FindFirstChild("Head")
            local Root = Player.Character:FindFirstChild("HumanoidRootPart")
            
            if Head and Root then
                local Pos, OnScreen = Camera:WorldToViewportPoint(Root.Position)
                local Center = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
                local Dist2D = (Vector2.new(Pos.X, Pos.Y) - Center).Magnitude
                
                local IsEnemy = not (LocalPlayer.Team and Player.Team and LocalPlayer.Team == Player.Team)
                
                if Dist2D < Config.FOV and IsEnemy and OnScreen then
                    local Dist3D = (Camera.CFrame.Position - Root.Position).Magnitude
                    if Dist3D < MinDist then
                        MinDist = Dist3D
                        Closest = Head
                    end
                end
            end
        end
    end
    return Closest
end

function ApplySilentAim()
    if not IsActive then return end
    
    local TargetHead = GetNearestTarget()
    if TargetHead then
        CurrentTarget = TargetHead.Parent.Parent.Name
        StatusTxt.Text = "LOCKED: " .. CurrentTarget
        
        -- Silent Aim via Mouse Manipulation (Zoom Safe)
        local ScreenPos, OnScreen = Camera:WorldToViewportPoint(TargetHead.Position)
        if OnScreen then
            local Mouse = LocalPlayer:GetMouse()
            Mouse.X = ScreenPos.X
            Mouse.Y = ScreenPos.Y
        end
    else
        CurrentTarget = nil
        StatusTxt.Text = "Scanning Clean Area..."
    end
end

-- ================= INITIALIZATION =================
Players.PlayerAdded:Connect(function(P)
    P.CharacterAdded:Connect(function() 
        task.wait(0.5)
        CreateESP(P) 
    end)
    if P.Character then CreateESP(P) end
end)

Players.PlayerRemoving:Connect(function(P)
    if ESP_Cache[P] then
        for _, Obj in pairs(ESP_Cache[P]) do if Obj.Remove then Obj:Remove() end end
        ESP_Cache[P] = nil
    end
end)

for _, P in pairs(Players:GetPlayers()) do if P.Character then CreateESP(P) end end

-- ================= MAIN LOOP =================
RunService.RenderStepped:Connect(function()
    Camera = workspace.CurrentCamera
    UpdateESP()
    ApplySilentAim()
end)

print("[Hanami] Eclipse System Loaded. Zero Clutter Active.")
