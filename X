-- Services
local playersService = game:GetService("Players")
local runService = game:GetService("RunService")
local userInputService = game:GetService("UserInputService")
local coreGui = game:GetService("CoreGui")

-- State Engine Pipeline
local localPlayer = playersService.LocalPlayer
local currentTab = "Movement"
local TOGGLE_KEY = Enum.KeyCode.RightControl
local features = {
	Flight = false,
	Noclip = false,
	InfJump = false,
	WalkSpeed = 16,
	JumpPower = 50,
	KillAura = false
}

local flightInstances = {}

-- Clean duplicate stack frames before rendering
local oldUi = coreGui:FindFirstChild("Inferno_V5_Perfect_Fixed")
if oldUi then oldUi:Destroy() end

-- UI Core Container
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "Inferno_V5_Perfect_Fixed"
screenGui.ResetOnSpawn = false
screenGui.Parent = coreGui

-- Responsive Main Window (Scale Based Framework)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0.55, 0, 0.55, 0) -- Responsive Scale Layout
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 16, 26)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

-- Absolute Aspect Ratio Locking (Prevents stretching/flattening)
local aspectConstraint = Instance.new("UIAspectRatioConstraint")
aspectConstraint.AspectRatio = 1.6 -- Locked wide screen ratio
aspectConstraint.Parent = mainFrame

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(130, 60, 220)
mainStroke.Thickness = 2
mainStroke.Parent = mainFrame

-- Left Navbar Frame
local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0.28, 0, 1, 0)
sidebar.BackgroundColor3 = Color3.fromRGB(28, 22, 36)
sidebar.BorderSizePixel = 0
sidebar.Parent = mainFrame

local sideCorner = Instance.new("UICorner")
sideCorner.CornerRadius = UDim.new(0, 12)
sideCorner.Parent = sidebar

-- Header Label Branding
local brandLabel = Instance.new("TextLabel")
brandLabel.Size = UDim2.new(1, 0, 0.15, 0)
brandLabel.BackgroundTransparency = 1
brandLabel.Text = "INFERNO v5.0"
brandLabel.TextColor3 = Color3.fromRGB(190, 120, 255)
brandLabel.Font = Enum.Font.GothamBold
brandLabel.TextSize = 16
brandLabel.Parent = sidebar

-- Navigation List Processing
local navContainer = Instance.new("Frame")
navContainer.Size = UDim2.new(1, 0, 0.8, 0)
navContainer.Position = UDim2.new(0, 0, 0.18, 0)
navContainer.BackgroundTransparency = 1
navContainer.Parent = sidebar

local navList = Instance.new("UIListLayout")
navList.Padding = UDim.new(0, 8)
navList.HorizontalAlignment = Enum.HorizontalAlignment.Center
navList.Parent = navContainer

-- Content Safe Zone Panel
local contentArea = Instance.new("ScrollingFrame")
contentArea.Size = UDim2.new(0.68, 0, 0.9, 0)
contentArea.Position = UDim2.new(0.3, 0, 0.05, 0)
contentArea.BackgroundTransparency = 1
contentArea.BorderSizePixel = 0
contentArea.ScrollBarThickness = 3
contentArea.ScrollBarImageColor3 = Color3.fromRGB(130, 60, 220)
contentArea.CanvasSize = UDim2.new(0, 0, 0, 0)
contentArea.Parent = mainFrame

local uiList = Instance.new("UIListLayout")
uiList.Padding = UDim.new(0, 10)
uiList.SortOrder = Enum.SortOrder.LayoutOrder
uiList.Parent = contentArea

uiList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	contentArea.CanvasSize = UDim2.new(0, 0, 0, uiList.AbsoluteContentSize.Y + 20)
end)

local renderedElements = {}
local function clearContentArea()
	for _, element in ipairs(renderedElements) do element:Destroy() end
	table.clear(renderedElements)
end

-- Premium Components Dynamic Sizing Creators
local function createToggle(name, featureKey)
	local row = Instance.new("Frame")
	row.Size = UDim2.new(1, -10, 0, 48) -- Absolute row standard constraint
	row.BackgroundColor3 = Color3.fromRGB(34, 28, 44)
	row.BorderSizePixel = 0
	row.Parent = contentArea
	table.insert(renderedElements, row)
	
	local rowCorner = Instance.new("UICorner")
	rowCorner.CornerRadius = UDim.new(0, 8)
	rowCorner.Parent = row

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.7, 0, 1, 0)
	label.Position = UDim2.new(0, 14, 0, 0)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextColor3 = Color3.fromRGB(240, 240, 240)
	label.Font = Enum.Font.GothamMedium
	label.TextSize = 13
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = row

	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 60, 0, 26)
	btn.Position = UDim2.new(1, -74, 0, 11)
	btn.BackgroundColor3 = features[featureKey] and Color3.fromRGB(130, 60, 220) or Color3.fromRGB(52, 44, 64)
	btn.Text = features[featureKey] and "ON" or "OFF"
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 11
	btn.Parent = row
	
	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 6)
	btnCorner.Parent = btn

	btn.MouseButton1Click:Connect(function()
		features[featureKey] = not features[featureKey]
		btn.Text = features[featureKey] and "ON" or "OFF"
		btn.BackgroundColor3 = features[featureKey] and Color3.fromRGB(130, 60, 220) or Color3.fromRGB(52, 44, 64)
	end)
end

local function createSlider(name, featureKey, min, max)
	local row = Instance.new("Frame")
	row.Size = UDim2.new(1, -10, 0, 58)
	row.BackgroundColor3 = Color3.fromRGB(34, 28, 44)
	row.BorderSizePixel = 0
	row.Parent = contentArea
	table.insert(renderedElements, row)
	
	local rowCorner = Instance.new("UICorner")
	rowCorner.CornerRadius = UDim.new(0, 8)
	rowCorner.Parent = row

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.8, 0, 0, 25)
	label.Position = UDim2.new(0, 14, 0, 6)
	label.BackgroundTransparency = 1
	label.Text = name .. ": " .. tostring(features[featureKey])
	label.TextColor3 = Color3.fromRGB(240, 240, 240)
	label.Font = Enum.Font.GothamMedium
	label.TextSize = 12
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = row

	local sliderBar = Instance.new("Frame")
	sliderBar.Size = UDim2.new(1, -28, 0, 5)
	sliderBar.Position = UDim2.new(0, 14, 0, 38)
	sliderBar.BackgroundColor3 = Color3.fromRGB(62, 52, 76)
	sliderBar.BorderSizePixel = 0
	sliderBar.Parent = row

	local sliderFill = Instance.new("Frame")
	local initPercent = (features[featureKey] - min) / (max - min)
	sliderFill.Size = UDim2.new(initPercent, 0, 1, 0)
	sliderFill.BackgroundColor3 = Color3.fromRGB(130, 60, 220)
	sliderFill.BorderSizePixel = 0
	sliderFill.Parent = sliderBar

	local sliderBtn = Instance.new("TextButton")
	sliderBtn.Size = UDim2.new(0, 12, 0, 12)
	sliderBtn.Position = UDim2.new(initPercent, -6, 0, -4)
	sliderBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	sliderBtn.Text = ""
	sliderBtn.Parent = sliderBar
	
	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(1, 0)
	btnCorner.Parent = sliderBtn

	local dragging = false
	sliderBtn.MouseButton1Down:Connect(function() dragging = true end)
	userInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)

	runService.RenderStepped:Connect(function()
		if dragging then
			local mousePos = userInputService:GetMouseLocation().X
			local barPos = sliderBar.AbsolutePosition.X
			local barWidth = sliderBar.AbsoluteSize.X
			local percentage = math.clamp((mousePos - barPos) / barWidth, 0, 1)
			
			sliderFill.Size = UDim2.new(percentage, 0, 1, 0)
			sliderBtn.Position = UDim2.new(percentage, -6, 0, -4)
			
			local value = math.floor(min + (percentage * (max - min)))
			features[featureKey] = value
			label.Text = name .. ": " .. tostring(value)
		end
	end)
end

local function renderView()
	clearContentArea()
	if currentTab == "Movement" then
		createToggle("Enable Flight System", "Flight")
		createToggle("Noclip Bypass Mode", "Noclip")
		createToggle("Infinite Air Jump", "InfJump")
		createSlider("WalkSpeed Modifier", "WalkSpeed", 16, 250)
		createSlider("JumpPower Modifier", "JumpPower", 50, 300)
	elseif currentTab == "Combat" then
		createToggle("Brutal Kill Aura", "KillAura")
	end
end

local navButtons = {}
local function createNavButton(name, layoutOrder)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 135, 0, 38)
	btn.BackgroundColor3 = currentTab == name and Color3.fromRGB(48, 36, 64) or Color3.fromRGB(34, 28, 44)
	btn.Text = name
	btn.TextColor3 = currentTab == name and Color3.fromRGB(210, 140, 255) or Color3.fromRGB(180, 180, 180)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 12
	btn.LayoutOrder = layoutOrder
	btn.Parent = navContainer
	
	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 6)
	btnCorner.Parent = btn
	
	navButtons[name] = btn

	btn.MouseButton1Click:Connect(function()
		currentTab = name
		for tName, tBtn in pairs(navButtons) do
			tBtn.BackgroundColor3 = currentTab == tName and Color3.fromRGB(48, 36, 64) or Color3.fromRGB(34, 28, 44)
			tBtn.TextColor3 = currentTab == tName and Color3.fromRGB(210, 140, 255) or Color3.fromRGB(180, 180, 180)
		end
		renderView()
	end)
end

createNavButton("Movement", 0)
createNavButton("Combat", 1)

-- Hard-Locked Flight Engine Core (Using Body Forces to bypass fall resets)
local function handleFlightPhysics(root, state)
	if state then
		if not flightInstances.Velocity then
			local bVel = Instance.new("BodyVelocity")
			bVel.MaxForce = Vector3.new(1e5, 1e5, 1e5)
			bVel.Velocity = Vector3.new(0, 0, 0)
			bVel.Parent = root
			flightInstances.Velocity = bVel
			
			local bGyro = Instance.new("BodyGyro")
			bGyro.MaxTorque = Vector3.new(1e5, 1e5, 1e5)
			bGyro.CFrame = root.CFrame
			bGyro.Parent = root
			flightInstances.Gyro = bGyro
		end
		
		local camera = workspace.CurrentCamera
		local moveDir = Vector3.new(0,0,0)
		
		if userInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camera.CFrame.LookVector end
		if userInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camera.CFrame.LookVector end
		if userInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camera.CFrame.RightVector end
		if userInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camera.CFrame.RightVector end
		if userInputService:IsKeyDown(Enum.KeyCode.E) then moveDir = moveDir + Vector3.new(0, 1, 0) end
		if userInputService:IsKeyDown(Enum.KeyCode.Q) then moveDir = moveDir - Vector3.new(0, 1, 0) end
		
		flightInstances.Velocity.Velocity = moveDir.Unit * features.WalkSpeed
		if moveDir.Magnitude == 0 then flightInstances.Velocity.Velocity = Vector3.new(0,0,0) end
		flightInstances.Gyro.CFrame = camera.CFrame
	else
		if flightInstances.Velocity then flightInstances.Velocity:Destroy() flightInstances.Velocity = nil end
		if flightInstances.Gyro then flightInstances.Gyro:Destroy() flightInstances.Gyro = nil end
	end
end

-- Core Engine Runtime Tracking Thread
runService.Heartbeat:Connect(function()
	local char = localPlayer.Character
	if not char then return end
	
	local hum = char:FindFirstChildOfClass("Humanoid")
	local root = char:FindFirstChild("HumanoidRootPart")
	
	if hum and not features.Flight then
		hum.WalkSpeed = features.WalkSpeed
		hum.JumpPower = features.JumpPower
	end

	if root then
		handleFlightPhysics(root, features.Flight)
	end

	if features.Noclip then
		for _, part in ipairs(char:GetDescendants()) do
			if part:IsA("BasePart") then part.CanCollide = false end
		end
	end
end)

userInputService.JumpRequest:Connect(function()
	if features.InfJump and localPlayer.Character then
		local hum = localPlayer.Character:FindFirstChildOfClass("Humanoid")
		if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
	end
end)

userInputService.InputBegan:Connect(function(input, gp)
	if gp then return end
	if input.KeyCode == TOGGLE_KEY then mainFrame.Visible = not mainFrame.Visible end
end)

renderView()
print("[INFERNO-V5] Absolute Aspect Engine Loaded. Flight System Override Hooked.")
